name: Import validated source
on:
  workflow_dispatch:
  push:
    paths: [nlquery-source.zip]
permissions:
  contents: write
jobs:
  import:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Verify and unpack exact validated source
        run: |
          python - <<'PY'
          import hashlib, pathlib, zipfile
          archive = pathlib.Path('nlquery-source.zip')
          assert hashlib.sha256(archive.read_bytes()).hexdigest() == '378e21f413638a65bf007f9a7e69a830a6683c631bb6cf66604edaccd6f4d221'
          with zipfile.ZipFile(archive) as z:
              assert all(not pathlib.PurePosixPath(n).is_absolute() and '..' not in pathlib.PurePosixPath(n).parts and not n.startswith('.github/') for n in z.namelist())
              z.extractall('.')
          archive.unlink()
          PY
      - run: python -m pip install -e ".[dev,postgres,neo4j,mongo,redis,mysql,openai,anthropic,yaml]"
      - run: ruff check .
      - run: ruff format --check .
      - run: mypy src
      - run: pytest --cov=nlquery
      - run: python -m build
      - run: python -m pip check
      - name: Commit tested source tree
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add -A
          git commit -m 'Fix SQLite portability, improve interpretation guidance and embed manual fonts'
          git push origin HEAD:main
